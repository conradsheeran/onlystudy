name: onlystudy
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  qr_flutter: ^4.1.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1 # 配合后续可能的缓存需求
  video_player: ^2.10.1
  chewie: ^1.13.0
  wakelock_plus: ^1.4.0
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  sqflite: ^2.4.2
  path: ^1.9.1
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
